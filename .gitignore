# Dependencies
node_modules/

# Build output
dist/

# Environment / secrets
.env
.env.local

# Runtime data (sessions are credentials)
data/
storageState.json

# Scraped user data (per-account; never commit)
scraped/

# Personal assistant config (name, matrícula, per-item overrides)
apps/server/config/profile.json
apps/server/config/overrides.json

# Playwright artifacts (screenshots, snapshots, traces)
.playwright-mcp/
playwright-report/
test-results/
blob-report/
playwright/.cache/
playwright/.auth/

# Logs
*.log
logs/

# OS
.DS_Store
